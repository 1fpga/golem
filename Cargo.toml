[workspace]
members = [
    "cyclone-v",
    "de10-nano",
    "golem-db",
    "golem",
    "junior",
    "mister-fpga",
    "senior",
]
resolver = "2"

[profile.release]
lto = "fat"
incremental = false
overflow-checks = false
panic = "abort"
strip = "symbols"
